server:
  port: 9092
  
spring:
  application:
    name: CONFIG-SERVER
    
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Bhanupratapsingh1998/microservice-configuration
          clone-on-start: true